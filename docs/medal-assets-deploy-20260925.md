# Medal asset deployment

All 18 new medal images are present under `public/badges/` in six series directories. This documentation-only commit requests a fresh deployment of the current `main` branch after the image uploads.
